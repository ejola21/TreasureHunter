// models/ranking_entry.dart — RankingEntry.swift 이식.
class RankingEntry {
  final int id;
  final String userName;
  final String record;
  const RankingEntry({required this.id, required this.userName, required this.record});
}
